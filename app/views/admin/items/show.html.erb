<h2>商品詳細</h2>

<%= image_tag @item.get_image %>
  <p><%= image_tag @item.get_image(100,100) %></p>

<tr>
  <td>商品名</td>
  <td><%= @item.name %></td>
</tr>

<tr>
  <td>商品説明</td>
  <td><%= @item.description %></td>
</tr>

<tr>
  <td>ジャンル</td>
  <td><%= @item.genre_id %></td>
</tr>

<tr>
  <td>税込価格</br>（税抜価格）</td>
  <td><%= @item.price %>（<%= @item.tax_exclude_price %>）</td>
</tr>

<tr>
  <td>販売ステータス</td>
  <td><%= @item.sales_status %></td>
</tr>

<th>
  <%= link_to edit_admin_item_path(@item.id) do %>
    <div class="btn btn-sm btn-success">編集する</div>
  <% end %>
</th>
