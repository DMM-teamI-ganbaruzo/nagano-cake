<h1>Public::Genres#show</h1>
<p>Find me in app/views/public/genres/show.html.erb</p>
<div class="item-index-box">

    <!-- ジャンル選択のサイドバー -->

    <!-- ジャンル選択のサイドバー -->
    <div class="item-index">
    <h3><%= @genre.name %>一覧</h3>
        <table>
          <tbody>
            <tr class="item-box">
              <% @genre.items.each do |item| %>
                <td class="item">
                    <%= link_to item_path(item.id) do %>
                      <%= item.name %><br>

                    <% end %>
                </td>
              <% end %>
            </tr>
          </tbody>
        </table>
    </div>
</div>