<table class="table", border="1">
 <thead>
  <tr>
   <th>
     ジャンル検索
   </th>
  </tr>
 </thead>
 <tbody>
  <tr>
   <td>
     <%= link_to "全商品", items_path %>
   </td>
  </tr>
     <% genres.each do |genre| %>
      <tr><td><%= link_to genre.name, genre_path(genre.id) %></td></tr>
     <% end %>
 </tbody>
</table>